#ifndef RECVPROCESS_H
#define RECVPROCESS_H

#include <QObject>

class recvprocess : public QObject
{
    Q_OBJECT
public:
    explicit recvprocess(QObject *parent = 0);

signals:

public slots:
};

#endif // RECVPROCESS_H
